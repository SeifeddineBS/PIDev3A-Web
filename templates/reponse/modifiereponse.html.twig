


{% block body %}
    <div class="container mt-4">
        <h1>Editer votre Reponse!</h1>
        {{ form_start(form) }}


        <div class="row">
            <div class="col-md-1"> {{  form_row(form.reponse) }}</div>
        </div>

        {{ form_widget(form) }}
        <button class="btn btn-primary"> Sauvgarder</button>
        {{ form_end(form) }}


    </div>

{% endblock %}