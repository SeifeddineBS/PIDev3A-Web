{% extends 'Back/base.html.twig' %}
{% block content %}

    <div class="container mt -4">
        <h3>Gérer Les Contacts</h3>
        <br>

        <div id="Table">
            <table id="tableObj" class="table table-striped">
                <thead>
                <tr>

                    <th>Type</th>
                    <th>email</th>
                    <th>message</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>
                {% for contact in contacts %}
                <tr>
                    <td>{{ contact.type }}</td>
                    <td>{{ contact.email}}</td>
                    <td>{{ contact.champ }}</td>

                    <td>
                        <a href="{{ path('supprimerContact', {id: contact.id}) }}" class="btn btn-danger" >Supprimer</a>
                    </td>
                </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}


